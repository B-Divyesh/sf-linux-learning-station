[
  {
    "id": "six-free-activities",
    "claim": "Six core learning activities are available without a purchase.",
    "where": "Landing page, station board, README",
    "test": "npm run test:e2e -- --grep @claim:six-free-activities",
    "sandbox": "Fresh mobile browser context; complete setup and count the six activity slabs."
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "README and Adult tools install copy",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "Fresh browser context; set up, wait for service-worker control, set the context offline, then reload."
  },
  {
    "id": "demo-sandbox",
    "claim": "Sample data is isolated from the real station.",
    "where": "First-screen demo action and demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "Fresh browser context; enter /demo, verify seeded progress, reset it, then return to an unconfigured real station."
  },
  {
    "id": "local-only",
    "claim": "Core activity progress stays on this computer and is not sent to us.",
    "where": "Landing page, Privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "Record every request during setup and a completed core round; assert every request is same-origin."
  },
  {
    "id": "json-export",
    "claim": "Adults can export local progress as JSON and malformed imports are rejected.",
    "where": "Adult tools and Privacy page",
    "test": "npm run test:e2e -- --grep @claim:json-export",
    "sandbox": "Fresh browser context; export progress, inspect its settings, then attempt to import a partial attempt."
  }
]
